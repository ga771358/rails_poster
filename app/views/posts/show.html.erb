<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @posts.user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @posts.user.email %>
</p>

<p>
  <strong>Title:</strong>
  <%= @posts.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @posts.content %>
</p>
<strong>Content:</strong>
<table>
  <% @posts.comments.each do |p|%>
    <tr>
      <td><%= p.comment %></td>
      <td><%= link_to 'Edit', edit_comment_path(p,:p_id => params[:id]) %></td>
      <td><%= link_to 'Delete', comment_path(p),:method => :delete %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Comment', new_comment_path(:p_id => params[:id]) %>

