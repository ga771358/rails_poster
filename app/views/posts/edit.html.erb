<%= form_for @posts , :url => post_path(@posts),:method => :patch do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>

<%= f.submit "Update" %>
<% end %>

<%= link_to 'Back', user_path(@posts.user_id) %>