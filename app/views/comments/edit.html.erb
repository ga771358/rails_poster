<%= form_for @comments , :url => comment_path(@comments),:method => :patch do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>

<%= f.submit "Update" %>
<% end %>

<%= link_to 'Back', post_path(@comments.post_id) %>